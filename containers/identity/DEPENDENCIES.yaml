depends_on:
  - kernel
  - governance
forbidden:
  - [list domain-specific forbidden dependencies from management/DEPENDENCY_GRAPH.yaml]

Note: See management/DEPENDENCY_GRAPH.yaml for authoritative dependency rules.
